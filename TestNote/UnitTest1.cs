using Note;

namespace TestNote

{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
        }

        [Test] 
        public void Test()
        {   
            ListNote listNote = new ListNote();
          //  Assert.AreEqual(8, listNote.GetNote());
        }
    }
}